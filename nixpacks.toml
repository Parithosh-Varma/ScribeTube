[phases.setup]
nixPkgs = ["python3", "python3Packages.pip", "python3Packages.setuptools"]

[phases.install]
cmds = ["python3 -m venv /app/.venv && /app/.venv/bin/pip install -r requirements.txt"]

[start]
cmd = "/app/.venv/bin/gunicorn server.server:app"
